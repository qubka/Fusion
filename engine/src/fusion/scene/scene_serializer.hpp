#pragma once

#include "scene.hpp"

namespace fe {
    class Scene;

    class SceneSerializer {
    public:
        /*static void Serialize(const std::filesystem::path& filename);
        static bool Deserialize(const std::filesystem::path& filename);

    private:*/
    };
}